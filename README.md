# Environment-Audit--LUA-
Environment Audit—LUA is a lightweight, strict diagnostic suite built to test Luau environments, security boundaries, and API coverage. Designed with accuracy over artificially inflated scores, it checks core UNC baselines, metatable hooks, reflection functions, rendering libraries, and file system sandboxing without generating false positives.
